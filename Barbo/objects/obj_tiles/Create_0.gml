// an array of tiles that are connected in a diamond grid
// we can assumr top right is North and bottom right is East

//  a grid of 1024 tiles
//
// we start on a tile in a set location (or randomly generated)


tileID=-1 // the tile ID to use (sprite)

for (xx=0;xx>32;xx++)
	{
	for (yy=0;yy>32;yy++)
		{
		tileArray[xx,yy]=tileID
		}
	}
	
	


//now define the one we are on

		
